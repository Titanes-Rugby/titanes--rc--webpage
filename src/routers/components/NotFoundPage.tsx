const NotFoundPage = () => (
	<main className="flex min-h-screen items-center justify-center bg-white px-6">
		<div className="text-center">
			<p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary-500">404</p>
			<h1 className="mt-2 text-3xl font-semibold text-primary-900">Pagina no encontrada</h1>
		</div>
	</main>
);

export default NotFoundPage;
